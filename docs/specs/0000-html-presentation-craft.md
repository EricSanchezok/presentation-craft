# HTML Presentation Craft

Artifact-Version: 1
Status: Implemented

## Intent

Implement the user's explicitly approved presentation-craft plan of 2026-09-11: a reusable pure-HTML presentation skill in a new local Git repository, initialized with repo-seed. The user approval is the implementation request containing the full plan. Existing projects remain unchanged; the initial scope is local HTML authoring without global installation.

## Contract

The skill supports new work, targeted refinement, critique and packaging across presentation genres. It contains its own references, starter and necessary scripts. One deck definition drives playback and notes. Every beat is directly reachable, reversible and interruption-safe. Motion respects reduced-motion preferences. Static print preserves complete information. Generated receiver output has one clear HTML entry with embedded playback resources; verification limitations are recorded honestly.

## Plan

Capture generalizable case decisions, initialize governance, author references, implement the independent starter, build three different example tasks, verify behavior and inspect rendered output, then package the skill and examples.

## Verification

Use focused navigation and packaging tests, compile each copied example, exercise its delivered HTML in Chromium, inspect screenshots and rendered print output, validate the skill and repository gates, and inspect archive contents after extraction.

## Evidence

[Verification record](../verification.md), [runtime tests](../../tests/core.test.mjs), [browser checks](../../tests/browser.mjs), and [skill reference integrity](../../scripts/check-skill.mjs) document the delivered contract and explicit limits.
