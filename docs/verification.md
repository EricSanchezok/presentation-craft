# 验证记录

日期：2026-09-11。环境：macOS、Node 22.23.2、Playwright 1.62.1、Chromium 151。验证对象是独立 starter 以及四套生成的 HTML 演示。

## 实际运行结果

| 检查 | 结果与证据 |
|---|---|
| Skill 结构与引用 | skill-creator 校验通过；主入口、参考资料和示例指南的必需链接均在 skill 内 |
| 导航与独立创建 | 8 个 Node 测试通过：双向遍历、深链接、拒绝覆盖现有项目、完整 SVG 属性、catalog 示例独立创建、经符号链接调用的真实 CLI 及 HTML/CSS 打包 |
| 独立构建 | 四套示例分别复制 starter、安装锁定依赖、通过类型检查、lint 和构建 |
| 图片内嵌 | 大于 4 KB 的 SVG 经 JS import 与 CSS url 引入，真实构建后正确显示且无附加资源请求 |
| 浏览器播放 | 12 页、29 个状态：1080p 正向、720p 反向、逐状态直接进入、打断恢复、讲稿焦点、总览、计时、减少动态效果通过；705px 窗口无文字越界 |
| SVG 机制 | 镜片、路径、焦点与滤镜在快速打断后恢复正确；波形的噪声与控制声在各采样点叠加为绘出的残余 |
| 网络依赖 | 四套演示 HTTP 加载后均无附加资源请求，断网后仍可导航 |
| 实际打印 | 四份 PDF 各 3 页，均为 16:9；使用 Poppler 渲染全部 12 页并检查构图 |
| ZIP | Skill 与演示 ZIP 逐文件解压并核对 SHA-256；原始源码、生成讲稿、预览与分发包按同一 catalog 检查 |
| 仓库治理 | 已记录 repo-seed 文件状态，文档、占位符、manifest、spec、decision 与 postmortem gates 通过 |

可追溯数据见 [浏览器结果与 HTML 校验和](evidence/browser-report.json)。实际打印总览见 [十二页画面](../examples/previews/overview.jpg)。永久行为证据位于 [浏览器测试](../tests/browser.mjs)、[核心测试](../tests/core.test.mjs) 与 [素材构建测试](../tests/assets.mjs)。

## 内容与视觉评审

对最终打印的 12 页、各示例预览、机制页的起点和关键中间状态进行了实际查看。耳机外观、耳罩剖面、相机与镜片光路具有可识别的结构；控制信号与结果的数量关系一致。业务示例保留同尺度条形图与明确的虚构数据标签。教学示例用区间、候选卡片与判定记录保持推导完整，打印保留四次问题。

评审检查了页面重心、标签位置、连线端点、语义换行、主次强调以及静态信息完整性。SVG 元素数量和截图数量不作为“设计优秀”的自动证明。这些示例用于说明可迁移的设计判断，不是对所有受众的效果实验。

## Skill 指引的适用性审查

方法层按受众的理解任务组织。以下审查检查入口与参考资料能否给出不同形式的选择依据，未制作新的演示，也不代表独立 agent 的泛化效果实验。

| 任务探针 | 指引应支持的选择 | 语义审查结果 |
|---|---|---|
| 独立阅读的方案决策 | 同口径比较、可见取舍、页面自足；可以完全静态 | 内容选择、比较编码和阅读模式均有依据，无需套机制图 |
| 以照片为主的文化叙事 | 情境、视角、图像序列与情绪节奏 | 目标包含感受，构图允许全幅图与文字转折，无需制造数字图表 |
| 面向开发者的代码教学 | 代码、条件与执行状态；预测后揭示反馈 | 推导和互动有选型依据，无需把程序强行画成实物 |
| 展示不确定结论的研究报告 | 主要发现、可比证据、区间与替代解释 | 主张强度由证据决定，没有必须宣言式表达的要求 |
| 真实界面驱动的产品介绍 | 任务、关键操作、局部放大与可见反馈 | 截图与体验有独立表达路径，不依赖已有示例的图形词汇 |

审查同时检查了条件性措辞、案例加载时机和重复内容。具体示例用于按需借鉴实现；默认画幅、组件和主题属于底座接口。方法文档不以个别项目的页数、人物处理或具体类比约束新任务。

## 验证边界

没有完成文件协议的直接双击验证：会话浏览器环境拒绝了该入口，未绕过限制。HTTP 载入后断网导航与资源检查不能替代文件协议验证。未验证 Safari、Firefox、Windows、读屏器体验或真实投影；系统字体不保证跨操作系统相同字形。没有真实现场计时朗读，用时字段是讲述预算。四套场景在同一实现会话中应用 skill，未进行独立盲测。

打包脚本使用解析器检查构建资源，不可能静态证明任意未来 JavaScript 不会联网。修改内容、资产或运行逻辑后必须重新运行相关检查，不能沿用这份结果为新演示背书。新项目的构建只生成待补充的验证说明，浏览器检查完成后才写入实际结果。

## 公开交付

[下载目录](../downloads/README.md) 提供 skill 与示例 ZIP。包内素材、讲稿、说明及生成预览均为当前独立示例；源码和压缩包内容已作分发检查。初始公开提交不包含依赖缓存、工作输出、个人路径、账号凭据或其他项目内容。第三方许可保留，原创材料按 MIT 分发。
